version: 2
jobs:
  build:
    docker:
      - image: 'circleci/node:10'
    steps:
      - checkout
      - run:
          name: install
          command: npm install
      - run:
          name: test
          command: npm t
      - run:
          name: release
          command: npm run semantic-release || true
