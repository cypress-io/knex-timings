'use strict'

/* eslint-env mocha */
const knexTimings = require('.')
describe('knex-timings', () => {
  it('write this test', () => {
    console.assert(knexTimings, 'should export something')
  })
})
